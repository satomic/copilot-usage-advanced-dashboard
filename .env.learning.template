# Learning Environment Configuration
# Copy this file to .env.learning and fill in your own credentials
# DO NOT commit .env.learning to git (it's in .gitignore)

# =================================================================
# YOUR PERSONAL LEARNING ENVIRONMENT
# This is completely separate from the original demo environment
# =================================================================

# Your Elasticsearch Instance (Elastic Cloud)
ELASTICSEARCH_URL=https://your-cluster-id.us-central1.gcp.cloud.es.io:9243
ELASTICSEARCH_USER=elastic
ELASTICSEARCH_PASS=your-elasticsearch-password

# GitHub Personal Access Token with Copilot permissions
GITHUB_PAT=your_github_pat_here

# Test with a small organization first (not production)
# You can use: octodemo-copilot-standalone (the one in your API example)
ORGANIZATION_SLUGS=octodemo-copilot-standalone

# Your Grafana Instance (Grafana Cloud)  
GRAFANA_URL=https://your-org.grafana.net
GRAFANA_USERNAME=admin
GRAFANA_PASSWORD=your-grafana-password
GRAFANA_TOKEN=your-grafana-token

# Learning Environment Index Names (Prefixed to avoid conflicts)
INDEX_SEAT_INFO=learning_copilot_seat_info_settings
INDEX_SEAT_ASSIGNMENTS=learning_copilot_seat_assignments
INDEX_NAME_TOTAL=learning_copilot_usage_total
INDEX_NAME_BREAKDOWN=learning_copilot_usage_breakdown
INDEX_NAME_BREAKDOWN_CHAT=learning_copilot_usage_breakdown_chat
INDEX_USER_METRICS=learning_copilot_user_metrics
INDEX_USER_ADOPTION=learning_copilot_user_adoption

# Timezone for your learning
TZ=America/New_York

# Log settings
LOG_PATH=logs